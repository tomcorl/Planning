# Copier ce fichier en `.env` et remplir les valeurs pour activer Supabase.
# Sans ce fichier, l'application démarre en MODE LOCAL (données simulées dans le navigateur).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Forcer le mode local même avec des clés configurées : VITE_LOCAL_MODE=true
